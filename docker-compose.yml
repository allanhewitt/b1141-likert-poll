version: "3.9"
services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    ports:
      - "4000:4000"

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE: http://localhost:4000
    ports:
      - "5173:80"
    depends_on:
      - backend
